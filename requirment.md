Transform my existing website currently built with HTML into a React.js-based frontend, a Laravel-based backend, and a MySQL database. Ensure the migration maintains the same functionality, preserves all data, and improves performance and scalability. Provide a clear structure for the React components, Laravel routes, and database schema. Outline the steps for integration between the frontend and backend, including API creation using Laravel. Specify best practices for deployment and any additional tools required to optimize the workflow.